DEFAULT install
LABEL install
	KERNEL ubuntu-installer/amd64/linux
	APPEND auto initrd=ubuntu-installer/amd64/initrd.gz url=http://{{dashboardURL}}/host/{{host.id}}/preseed locale=en_US keyboard-configuration/layoutcode=us hostname={{host.name}} interface=auto -- quiet 
